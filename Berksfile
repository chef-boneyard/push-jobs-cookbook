site :opscode

metadata

group :integration do
  cookbook "apt"
  cookbook "yum"
  cookbook "opscode-push-jobs_test", :path => "./test/cookbooks/opscode-push-jobs_test"

  # Future, when/if minitest support for this cookbook is added
  #cookbook "minitest-handler"
end
